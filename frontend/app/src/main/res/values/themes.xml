<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base Theme -->
    <style name="Base.Theme.Frontend" parent="Theme.Material3.Light.NoActionBar">
        <!-- Fondo blanco para la app -->
        <item name="android:colorBackground">@color/white</item>
        <!-- Barra de estado blanca -->
        <item name="android:statusBarColor">@android:color/white</item>
        <!-- Iconos oscuros en la status bar -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Tu tema principal -->
    <style name="Theme.Frontend" parent="Base.Theme.Frontend" />

    <!-- Tema personalizado adicional (opcional si lo usas en otros sitios) -->
    <style name="Theme.EcoMarket" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@android:color/white</item> <!-- Corregido -->
        <item name="colorPrimaryVariant">@color/primary_light</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="colorSecondaryVariant">@color/accent</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="colorOnBackground">@color/black</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/white</item>
        <item name="android:statusBarColor">@android:color/white</item> <!-- Corregido -->
        <item name="android:windowLightStatusBar">true</item> <!-- Añadido -->
    </style>
</resources>
