<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:gravity="center">

        <!-- Icono de ubicación -->
        <ImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginBottom="24dp"
            android:src="@drawable/ic_location_permission"
            android:contentDescription="Icono de ubicación"
            android:background="@drawable/circle_background_primary"
            android:padding="24dp" />

        <!-- Título -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Optimización de Compras"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"
            android:fontFamily="@font/manrope_bold"
            android:gravity="center"
            android:layout_marginBottom="16dp" />

        <!-- Descripción -->
        <TextView
            android:id="@+id/status_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Para mostrarte los mejores productos cercanos, necesitamos tu ubicación"
            android:textSize="16sp"
            android:textColor="#666666"
            android:fontFamily="@font/manrope_regular"
            android:gravity="center"
            android:lineSpacingExtra="4dp"
            android:layout_marginBottom="24dp" />

        <!-- Información de ubicación (cuando se obtiene) -->
        <TextView
            android:id="@+id/location_info_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="14sp"
            android:textColor="#666666"
            android:fontFamily="@font/manrope_regular"
            android:gravity="center"
            android:lineSpacingExtra="2dp"
            android:background="@drawable/rounded_background_light"
            android:padding="16dp"
            android:layout_marginBottom="24dp"
            android:visibility="gone" />

        <!-- Beneficios -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="32dp"
            app:cardCornerRadius="16dp"
            app:cardElevation="4dp"
            android:backgroundTint="@android:color/white">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="¿Por qué necesitamos tu ubicación?"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="@android:color/black"
                    android:fontFamily="@font/manrope_bold"
                    android:layout_marginBottom="12dp" />

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="• Encontrar productores locales más cercanos\n• Calcular distancias reales para optimizar rutas\n• Reducir el impacto ambiental del transporte\n• Mostrar productos con mejor relación precio-distancia\n• Personalizar recomendaciones según tu zona"
                    android:textSize="14sp"
                    android:textColor="#666666"
                    android:fontFamily="@font/manrope_regular"
                    android:lineSpacingExtra="4dp" />

            </LinearLayout>

        </androidx.cardview.widget.CardView>

        <!-- Barra de progreso -->
        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:visibility="gone" />

        <!-- Botones -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center">

            <!-- Botón principal -->
            <Button
                android:id="@+id/btn_request_location"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:text="Obtener mi ubicación"
                android:textAllCaps="false"
                android:textSize="16sp"
                android:textStyle="bold"
                android:fontFamily="@font/manrope_bold"
                android:textColor="@android:color/white"
                android:background="@drawable/button_primary_background"
                android:layout_marginBottom="16dp"
                android:elevation="4dp" />

            <!-- Botón continuar (cuando se obtiene ubicación) -->
            <Button
                android:id="@+id/btn_continue"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:text="Continuar con optimización"
                android:textAllCaps="false"
                android:textSize="16sp"
                android:textStyle="bold"
                android:fontFamily="@font/manrope_bold"
                android:textColor="@android:color/white"
                android:background="@drawable/button_primary_background"
                android:layout_marginBottom="16dp"
                android:elevation="4dp"
                android:visibility="gone" />

            <!-- Botón secundario -->
            <Button
                android:id="@+id/btn_skip_location"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:text="Continuar sin ubicación"
                android:textAllCaps="false"
                android:textSize="14sp"
                android:fontFamily="@font/manrope_regular"
                android:textColor="#666666"
                android:background="@android:color/transparent" />

        </LinearLayout>

        <!-- Información adicional -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="💡 Sin ubicación, podrás ver todos los productos pero sin optimización de distancias"
            android:textSize="12sp"
            android:textColor="#999999"
            android:fontFamily="@font/manrope_regular"
            android:gravity="center"
            android:lineSpacingExtra="2dp"
            android:layout_marginTop="16dp" />

    </LinearLayout>
</ScrollView>









