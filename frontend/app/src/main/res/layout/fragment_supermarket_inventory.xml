<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F8F9FA"
    android:padding="16dp">

    <!--  INVENTARIO - STOCK BAJO -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Inventario actual"
        android:textStyle="bold"
        android:textSize="20sp"
        android:textColor="#222"
        android:layout_marginBottom="8dp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerSuggestions"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:layout_marginBottom="16dp"
        android:clipToPadding="false"
        android:padding="4dp"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#DDDDDD"
        android:layout_marginBottom="12dp" />

    <!-- 🔵 PEDIDOS (UNA SOLA LISTA) -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Historial de pedidos"
        android:textStyle="bold"
        android:textSize="20sp"
        android:textColor="#222"
        android:layout_marginBottom="8dp" />

    <!-- Filtro de búsqueda -->
    <com.google.android.material.textfield.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
        app:boxStrokeColor="#00C853"
        app:boxBackgroundColor="#FFFFFF"
        app:boxCornerRadiusTopStart="12dp"
        app:boxCornerRadiusTopEnd="12dp"
        app:boxCornerRadiusBottomStart="12dp"
        app:boxCornerRadiusBottomEnd="12dp">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/search_order"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="🔍 Buscar por cliente, ID..."
            android:background="@android:color/transparent"/>
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Chips de filtros -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:scrollbars="none">

        <com.google.android.material.chip.ChipGroup
            android:id="@+id/chip_group_filters"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:singleSelection="false"
            app:selectionRequired="false"
            android:padding="2dp">

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_estado_pendiente"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="Pendientes"
                android:textSize="12sp"
                android:textColor="@android:color/black"
                app:chipBackgroundColor="@android:color/white"
                app:chipStrokeColor="#E0E0E0"
                app:chipStrokeWidth="1dp"
                app:chipCornerRadius="16dp" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_estado_entregado"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="Entregados"
                android:textSize="12sp"
                android:textColor="@android:color/black"
                app:chipBackgroundColor="@android:color/white"
                app:chipStrokeColor="#E0E0E0"
                app:chipStrokeWidth="1dp"
                app:chipCornerRadius="16dp" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_fecha_reciente"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="Más recientes"
                android:textSize="12sp"
                android:textColor="@android:color/black"
                app:chipBackgroundColor="@android:color/white"
                app:chipStrokeColor="#E0E0E0"
                app:chipStrokeWidth="1dp"
                app:chipCornerRadius="16dp" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_fecha_antigua"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="Más antiguos"
                android:textSize="12sp"
                android:textColor="@android:color/black"
                app:chipBackgroundColor="@android:color/white"
                app:chipStrokeColor="#E0E0E0"
                app:chipStrokeWidth="1dp"
                app:chipCornerRadius="16dp" />
        </com.google.android.material.chip.ChipGroup>
    </HorizontalScrollView>

    <!-- Recycler de pedidos (único listado) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_order_history"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#FFFFFF"
        android:padding="8dp"
        android:elevation="2dp"
        android:clipToPadding="false" />
</LinearLayout>
