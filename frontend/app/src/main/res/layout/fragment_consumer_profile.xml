<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="0dp">

        <!-- Botón volver -->
        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginTop="12dp"
            android:layout_marginStart="12dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_arrow_back"
            android:contentDescription="Volver"
            android:tint="@android:color/black"/>

        <!-- Título -->
        <TextView
            android:id="@+id/profileTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Perfil"
            android:textSize="32sp"
            android:textStyle="bold"
            android:layout_marginTop="8dp"
            android:layout_marginStart="24dp"
            android:layout_marginBottom="8dp"
            android:fontFamily="sans-serif"/>

        <!-- Foto y nombre -->
        <LinearLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:paddingStart="24dp"
            android:paddingEnd="24dp"
            android:paddingBottom="16dp">

            <ImageView
                android:id="@+id/profileImage"
                android:src="@drawable/ic_user"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:background="@drawable/bg_circle"
                android:scaleType="centerCrop"/>

            <LinearLayout
                android:orientation="vertical"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp">

                <TextView
                    android:id="@+id/profileName"
                    android:text="Nombre Apellido"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    android:fontFamily="sans-serif"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"/>

                <TextView
                    android:id="@+id/profileView"
                    android:text="Ver perfil"
                    android:textColor="@color/primary"
                    android:textSize="14sp"
                    android:fontFamily="sans-serif"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"/>
            </LinearLayout>
        </LinearLayout>

        <!-- Tarjeta informativa (opcional) -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="16dp"
            app:cardCornerRadius="16dp"
            app:cardElevation="2dp">

            <LinearLayout
                android:orientation="horizontal"
                android:padding="16dp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <TextView
                    android:text="Invita a tus amigos y gana puntos"
                    android:textSize="16sp"
                    android:fontFamily="sans-serif"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"/>

                <ImageView
                    android:src="@drawable/ic_gift"
                    android:layout_width="40dp"
                    android:layout_height="40dp"/>
            </LinearLayout>
        </androidx.cardview.widget.CardView>

        <!-- Ajustes -->
        <TextView
            android:text="Ajustes"
            android:textSize="18sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif"
            android:layout_marginStart="24dp"
            android:layout_marginBottom="8dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <!-- Opciones de ajustes -->
        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/white">

            <!-- Opción: Información personal -->
            <include layout="@layout/item_profile_option" android:id="@+id/optionPersonalInfo"/>
            <!-- Opción: Mis pedidos -->
            <include layout="@layout/item_profile_option" android:id="@+id/optionOrders"/>
            <!-- Opción: Métodos de pago -->
            <include layout="@layout/item_profile_option" android:id="@+id/optionPayments"/>
            <!-- Opción: Direcciones -->
            <include layout="@layout/item_profile_option" android:id="@+id/optionAddresses"/>
            <!-- Opción: Seguridad y acceso -->
            <include layout="@layout/item_profile_option" android:id="@+id/optionSecurity"/>
            <!-- Opción: Preferencias -->
            <include layout="@layout/item_profile_option" android:id="@+id/optionPreferences"/>
            <!-- Opción: Accesibilidad -->
            <include layout="@layout/item_profile_option" android:id="@+id/optionAccessibility"/>
        </LinearLayout>
    </LinearLayout>
</androidx.core.widget.NestedScrollView> 